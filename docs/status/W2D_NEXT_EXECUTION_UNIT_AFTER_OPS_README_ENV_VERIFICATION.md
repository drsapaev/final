# Next Execution Unit After Ops README and Env Verification

Recommended next step:

- completed via `docs/architecture/W2D_ENV_SETUP_GUIDE_VERIFICATION.md`

Why:

- the backend env guide has now been normalized as a dev-first entry doc with
  current caveats instead of a stale parallel env source of truth

Current next step now lives in:

- `docs/status/W2D_NEXT_EXECUTION_UNIT_AFTER_ENV_SETUP_GUIDE_VERIFICATION.md`

Required entry conditions:

- keep the slice docs-first unless a factual claim forces a tiny support-file
  correction
- verify env names and setup steps against current backend config and current
  templates before preserving them
- downgrade stale setup instructions instead of inventing a new env workflow
