# W2D Status Navigation Index Cleanup Plan

Scope:

- add a single status landing page for the current W2D doc stack
- keep the master plan as canonical and the backlog as the short execution
  index
- avoid broad archive rewrites; focus only on current navigation entry points

Expected outcome:

- one obvious first file for status re-entry
- clearer separation between:
  - canonical status
  - execution index
  - historical records
