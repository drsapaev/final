# AI Factory Doc-to-Code Matrix

| Claim ID | Classification | Evidence in current `origin/main` | Missing gap / nuance | Confidence | Recommendation |
|---|---|---|---|---|---|
| `ARCH-01` | `IMPLEMENTED_IN_MAIN` | Layered flow and domain modules are reflected in `backend/app/api/v1/endpoints/*`, `backend/app/services/*`, `backend/app/repositories/*`, and `backend/app/models/*`. | No meaningful gap found. | HIGH | Keep as done |
| `ARCH-02` | `IMPLEMENTED_IN_MAIN` | Postgres/Alembic SSOT is reinforced by migrations, runbooks, and the repo-wide backend setup. | No meaningful gap found. | HIGH | Keep as done |
| `QUEUE-01` | `IMPLEMENTED_IN_MAIN` | `backend/app/services/queue_service.py`, `backend/app/api/v1/api.py`, `backend/app/models/online_queue.py`, and current queue docs show the realtime queue core and Redis/WebSocket wiring. | None. | HIGH | Keep as done |
| `QUEUE-02` | `IMPLEMENTED_IN_MAIN` | `DailyQueue`, `OnlineQueueEntry`, and `QueueToken` exist; `OnlineDay` and `online_queue.py` are marked deprecated in the code/doc trail. | None. | HIGH | Keep as done |
| `QUEUE-03` | `IMPLEMENTED_DIFFERENTLY` | The queue SSOT is now embodied by `docs/QUEUE_SYSTEM_ARCHITECTURE.md`, `docs/runbooks/LOCAL_STAGING_ACCEPTANCE_RUNBOOK.md`, and the live queue code, not by the older migration/fix-plan docs. | Old docs remain historical evidence only. | HIGH | Archive the old docs, keep the current SSOT docs |
| `EMR-01` | `IMPLEMENTED_IN_MAIN` | `backend/app/api/v1/endpoints/appointment_flow.py`, `docs/runbooks/EMR_V2_HARD_CUTOVER_RUNBOOK.md`, and the roadmap all point to canonical `visit_id` EMR v2 with freeze/backfill/verification. | No product gap found in current main. | HIGH | Keep as done |
| `EMR-02` | `IMPLEMENTED_DIFFERENTLY` | The local-staging acceptance path exists as a runbook plus evidence logs, not as a runtime feature. | This is an operator process, not app code. | MEDIUM | Keep as current operator truth; do not turn into runtime work |
| `EMR-03` | `IMPLEMENTED_IN_MAIN` | Specialist EMR panels and `backend/app/api/v1/api.py` wire the v2 routes and canonical visit handling. | No meaningful gap found. | MEDIUM | Keep as done |
| `NOTIF-01` | `IMPLEMENTED_IN_MAIN` | `backend/app/api/v1/endpoints/notifications.py`, `backend/app/services/notification_platform_service.py`, `frontend/src/contexts/NotificationCenterContext.jsx`, and `frontend/src/contexts/NotificationWebSocketContext.jsx` show the backend-owned inbox and WS normalization. | No meaningful gap found. | HIGH | Keep as done |
| `NOTIF-02` | `IMPLEMENTED_IN_MAIN` | `frontend/src/__tests__/notificationGuardrails.test.js` and the notification WebSocket/context layer show the shared adapter path. | The guardrail is enforced by tests and the notification context, not by a dedicated runtime policy engine. | HIGH | Keep as done |
| `MSG-01` | `IMPLEMENTED_IN_MAIN` | `backend/app/core/messaging_contract.py`, `backend/app/ws/chat_ws.py`, and `backend/app/api/v1/endpoints/messages.py` prove versioned, reliable chat events with read/reconnect handling. | No meaningful gap found. | HIGH | Keep as done |
| `MSG-02` | `IMPLEMENTED_IN_MAIN` | `backend/app/services/messages_api_service.py`, `backend/app/api/v1/endpoints/notifications.py`, and the runbooks show current messaging/notification surfaces are aligned to backend responses. | Historical docs remain evidence only. | MEDIUM | Keep as done; archive historical contract docs as evidence |
| `QA-01` | `IMPLEMENTED_DIFFERENTLY` | `docs/PANEL_QA_CHECKLIST.md` and `.ai-factory/logs/PANEL_QA_IMPLEMENTATION_STATUS.md` show the resumable acceptance process is doc/runbook-driven. | There is no dedicated runtime QA state machine; the evidence is manual/runbook-based. | HIGH | Keep as current process, not product code |
| `QA-02` | `IMPLEMENTED_DIFFERENTLY` | `docs/runbooks/MESSAGING_QA_CHECKLIST.md` and `.ai-factory/logs/MESSAGING_AUDIT_STATUS.md` are execution evidence, not live product logic. | This is audit history, not a runtime feature. | HIGH | Keep as historical evidence only |
| `CI-01` | `IMPLEMENTED_IN_MAIN` | `.github/workflows/ci-cd-unified.yml`, `.github/workflows/role-system-check.yml`, and `docs/CI_GUARDRAILS.md` match the CI gate claims. | None. | HIGH | Keep as done |
| `CI-02` | `IMPLEMENTED_IN_MAIN` | `.github/workflows/security-scan.yml` emits the bandit/safety/pip-audit artifacts described by the docs. | None. | HIGH | Keep as done |
| `OPS-01` | `PARTIALLY_IMPLEMENTED` | `.github/workflows/load-testing.yml` and `docs/runbooks/LOAD_TESTING_RUNBOOK.md` prove load testing exists. | The broader SLO/capacity engineering story is documented, but not all current main evidence proves end-to-end budget enforcement for every critical group. | MEDIUM | Finish later if SLO enforcement is a priority |
| `OPS-02` | `PARTIALLY_IMPLEMENTED` | `docs/runbooks/OBSERVABILITY_SLA_RUNBOOK.md` and `docs/PRODUCTION_SECURITY.md` document metrics/alerts/logging. | The docs prove the contract, but the audit did not find a single dedicated proof artifact for all SLA loops. | MEDIUM | Add to future backlog if observability is still a focus |
| `OPS-03` | `DOC_ONLY_NOT_IN_CODE` | `docs/runbooks/VPS_HOST_ROLLOUT_RUNBOOK.md` exists, but current main does not prove the actual VPS promotion was executed. | This remains a rollout track, not a completed product capability. | HIGH | Put on a dedicated rollout track |
| `OPS-04` | `UNCLEAR_NEEDS_DEEPER_REVIEW` | The roadmap claims multi-clinic scale / interoperability completed. | The current audit did not find enough explicit code evidence to re-confirm the full tenant-isolation story. | LOW | Review separately before acting |

