# Эталонное поле: «Жалобы»

## Цель документа

Создать **образец идеального UX** для одного поля, который станет эталоном для всех остальных полей в SingleSheetEMR.

---

## Почему именно «Жалобы»?

1. **Первое поле** — задаёт тон всему приёму
2. **Свободный текст** — максимум вариаций ввода
3. **AI-релевантно** — здесь AI может реально помочь
4. **Клинически важно** — это начало диагностической цепочки

---

## Состояния поля

### 1. ПУСТО (Empty)

```
┌─────────────────────────────────────────────────────┐
│ ЖАЛОБЫ                                              │
├─────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────┐ │
│ │ Введите жалобы пациента...                     │ │
│ │ |                                              │ │ ← курсор мигает
│ │                                                 │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

**Поведение:**
- Placeholder серый, не мешает
- Auto-focus при открытии EMR (курсор уже здесь)
- Border нейтральный (#e5e5e5)
- Высота: 80px минимум

**Клавиатура:**
- Любая буква → начало ввода (состояние 2)
- Tab → переход к следующему полю
- Shift+Tab → переход назад (header/buttons)

**Мышь:**
- Клик → фокус (если не было)

---

### 2. В ПРОЦЕССЕ ВВОДА (Typing)

```
┌─────────────────────────────────────────────────────┐
│ ЖАЛОБЫ                                              │
├─────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────┐ │
│ │ Головная боль в затылке|                       │ │ ← врач печатает
│ │                                                 │ │
│ │                                                 │ │
│ └─────────────────────────────────────────────────┘ │
│   💾 Auto-save через 3 сек                          │ ← subtle hint
└─────────────────────────────────────────────────────┘
```

**Поведение:**
- Border синий (#007aff) — фокус
- Placeholder исчезает
- Текст чёрный (#1d1d1f)
- Textarea растягивается при необходимости

**Auto-save:**
- Debounce 3 секунды после последнего нажатия
- Subtle индикатор "Сохранено" (не блокирует)

**Клавиатура:**
- Enter → новая строка (это textarea)
- Tab → переход к следующему полю AND сохранение
- Escape → ничего (не закрываем)

**Мышь:**
- Клик вне поля → blur, сохранение

---

### 3. С AI-ПОДСКАЗКОЙ (AI Suggestion)

```
┌─────────────────────────────────────────────────────┐
│ ЖАЛОБЫ                                              │
├─────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────┐ │
│ │ Головная боль в затылке                        │ │
│ │                                                 │ │
│ └─────────────────────────────────────────────────┘ │
│                                                      │
│ ┌─ 💡 ─────────────────────────────────────────────┐ │
│ │ давящего характера, усиливается к вечеру,      │ │ ← AI предложение
│ │ сопровождается шумом в ушах                    │ │
│ │                          [✓ Принять] Alt+Enter │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

**Когда появляется:**
- После паузы ввода 1.5 сек
- Только если есть релевантный контекст
- Только если врач не отключил AI

**Поведение:**
- Появляется плавно (fade in 200ms)
- Не закрывает поле ввода
- Не мешает продолжать печатать

**Клавиатура:**
- Alt+Enter → принять подсказку (append к тексту)
- Escape → скрыть подсказку
- Продолжение печати → подсказка обновляется или исчезает

**Мышь:**
- Клик на [Принять] → append текст
- Клик вне → скрыть подсказку

**Важно:**
- AI **дополняет**, а не заменяет
- Подсказка insert'ится с ", " если текст не пустой
- После принятия — фокус остаётся в поле

---

### 4. ЗАПОЛНЕНО (Filled, not focused)

```
┌─────────────────────────────────────────────────────┐
│ ЖАЛОБЫ                                              │
├─────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────┐ │
│ │ Головная боль в затылке, давящего характера,   │ │
│ │ усиливается к вечеру, сопровождается шумом     │ │
│ │ в ушах. Длительность 3 дня.                    │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

**Поведение:**
- Border нейтральный (#e5e5e5)
- Текст чёрный, читаемый
- Высота адаптирована под контент
- Можно кликнуть для редактирования

**Клавиатура (при фокусе на другом поле):**
- Shift+Tab → вернуться и редактировать

**Мышь:**
- Клик → фокус, курсор в конец текста

---

### 5. ПОДПИСАНО (Signed / Read-only)

```
┌─────────────────────────────────────────────────────┐
│ ЖАЛОБЫ                                              │
├─────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────┐ │
│ │ Головная боль в затылке, давящего характера,   │ │ ← серый фон
│ │ усиливается к вечеру, сопровождается шумом     │ │
│ │ в ушах. Длительность 3 дня.                    │ │
│ └─────────────────────────────────────────────────┘ │
│                                    🔒 Подписано      │
└─────────────────────────────────────────────────────┘
```

**Поведение:**
- Background: #f5f5f7 (subtle gray)
- Текст: #636366 (secondary)
- Курсор: default (не text)
- Нет hover-эффектов

**Клавиатура:**
- Tab → пропускает поле (оно не editable)

**Мышь:**
- Клик → ничего (может показать tooltip "Документ подписан")

---

### 6. ОШИБКА / ВАЛИДАЦИЯ (Error)

```
┌─────────────────────────────────────────────────────┐
│ ЖАЛОБЫ                                              │
├─────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────┐ │
│ │                                                 │ │ ← пусто!
│ │                                                 │ │
│ └─────────────────────────────────────────────────┘ │
│   ⚠️ Жалобы обязательны для подписания              │
└─────────────────────────────────────────────────────┘
```

**Когда показывается:**
- При попытке подписать с пустым полем
- НЕ при обычном сохранении черновика

**Поведение:**
- Border: #ff3b30 (red)
- Сообщение под полем
- Фокус автоматически в это поле

**Важно:**
- Врач может сохранить черновик с пустыми полями
- Ошибка только при финальной подписи

---

## Карта переходов между состояниями

```
                    ┌──────────────┐
                    │   ПУСТО      │
                    └──────┬───────┘
                           │ ввод текста
                           ▼
                    ┌──────────────┐
              ┌─────│ В ПРОЦЕССЕ   │─────┐
              │     └──────┬───────┘     │
              │            │ пауза 1.5с  │ blur
              │            ▼             │
              │     ┌──────────────┐     │
              │     │ AI ПОДСКАЗКА │     │
              │     └──────┬───────┘     │
              │            │ accept/     │
              │            │ dismiss     │
              │            ▼             ▼
              │     ┌──────────────┐
              └────▶│ ЗАПОЛНЕНО    │◀────┘
                    └──────┬───────┘
                           │ подпись
                           ▼
                    ┌──────────────┐
                    │ ПОДПИСАНО    │
                    └──────────────┘
```

---

## Спецификация компонента

```typescript
interface ComplaintsFieldProps {
  // Data
  value: string;
  onChange: (value: string) => void;
  
  // State
  isEditable: boolean;        // false после подписи
  isFocused?: boolean;        // контролируемый фокус
  
  // AI
  aiSuggestion?: string;      // текущая подсказка
  onAcceptAI?: () => void;    // принять подсказку
  onDismissAI?: () => void;   // скрыть подсказку
  
  // Validation
  error?: string;             // сообщение ошибки
  
  // UX
  autoFocus?: boolean;        // фокус при mount
  onFieldTouch?: () => void;  // для telemetry
}
```

---

## CSS-спецификация

```css
/* Базовое состояние */
.complaints-field {
  min-height: 80px;
  padding: 12px 16px;
  border: 1px solid #e5e5e5;
  border-radius: 10px;
  font-size: 15px;
  line-height: 1.5;
  resize: vertical;
  transition: border-color 0.2s, box-shadow 0.2s;
}

/* Фокус */
.complaints-field:focus {
  border-color: #007aff;
  box-shadow: 0 0 0 3px rgba(0, 122, 255, 0.15);
  outline: none;
}

/* Ошибка */
.complaints-field--error {
  border-color: #ff3b30;
}

/* Подписано */
.complaints-field--readonly {
  background: #f5f5f7;
  color: #636366;
  cursor: default;
}
```

---

## Поведение AI-подсказки

### Триггер появления:
1. Врач вводит текст
2. Пауза ≥ 1.5 секунды
3. Текст ≥ 10 символов
4. AI включён в настройках

### Формат подсказки:
- Дополнение к существующему тексту
- Начинается с lowercase (будет append через ", ")
- Не повторяет то, что уже написано

### Принятие подсказки:
```javascript
const acceptSuggestion = () => {
  if (!aiSuggestion) return;
  
  const separator = value.trim().endsWith('.') ? ' ' : ', ';
  const newValue = value.trim() + separator + aiSuggestion;
  
  onChange(newValue);
  onDismissAI();
  // Фокус остаётся в поле, курсор в конец
};
```

---

## Валидация

### При сохранении черновика:
- Нет валидации
- Всё сохраняется

### При подписании:
```javascript
const validateForSigning = (emr) => {
  const errors = [];
  
  if (!emr.complaints?.trim()) {
    errors.push({ field: 'complaints', message: 'Жалобы обязательны' });
  }
  
  if (!emr.diagnosis?.trim()) {
    errors.push({ field: 'diagnosis', message: 'Диагноз обязателен' });
  }
  
  return errors;
};
```

---

## Keyboard Navigation Map

| Состояние | Key | Действие |
|-----------|-----|----------|
| Пусто | Any letter | Начать ввод |
| Пусто | Tab | Следующее поле |
| Ввод | Tab | Сохранить + следующее поле |
| Ввод | Shift+Tab | Сохранить + предыдущее поле |
| Ввод | Enter | Новая строка |
| Ввод | Escape | Ничего |
| AI показан | Alt+Enter | Принять подсказку |
| AI показан | Escape | Скрыть подсказку |
| AI показан | Любая буква | Продолжить ввод, AI обновляется |
| Подписано | Tab | Пропустить (не focusable) |

---

## Telemetry для поля «Жалобы»

```javascript
const complaintsMetrics = {
  // Время
  timeToFirstChar: ms,        // от открытия EMR до первого символа
  totalEditTime: ms,          // общее время в фокусе
  
  // Ввод
  characterCount: number,     // итоговое количество символов
  wordCount: number,          // слов
  editCount: number,          // сколько раз редактировал
  
  // AI
  aiSuggestionsShown: number, // сколько подсказок показано
  aiAcceptedCount: number,    // сколько принято
  aiDismissedCount: number,   // сколько скрыто
  aiAcceptRate: percent,      // % принятия
  
  // Паттерны
  usedTemplate: boolean,      // использовал шаблон
  pastedText: boolean,        // вставлял текст
};
```

---

## Примеры контента

### Минимально допустимо:
```
Головная боль
```

### Типовой кардиологический:
```
Головная боль в затылочной области, давящего характера, 
усиливается к вечеру, сопровождается шумом в ушах. 
Периодические перебои в работе сердца. 
Одышка при подъёме на 2 этаж.
```

### С AI-дополнением:
```
Головная боль в затылке ← врач ввёл
, давящего характера, усиливается к вечеру ← AI добавил
```

---

## Чек-лист качества поля

- [ ] Auto-focus работает при открытии EMR
- [ ] Placeholder виден, не мешает
- [ ] Border меняется при фокусе
- [ ] Tab переходит к следующему полю
- [ ] AI появляется через 1.5 сек
- [ ] Alt+Enter принимает AI
- [ ] Escape скрывает AI
- [ ] После подписи поле read-only
- [ ] Ошибка показывается при попытке подписать пустое
- [ ] Telemetry собирает метрики
