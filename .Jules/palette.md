## 2024-05-14 - Implementing `clearable` prop for MacOSInput\n**Learning:** The `clearable` prop was defined in the component signature but ignored (using `void clearable`). Implementing it improves the user experience by allowing a single click to clear the input, especially useful for search and form fields.\n**Action:** Add a small clear button inside the input wrapper when `clearable` is true and the input has a value. Needs to use standard `lucide-react` icons.
