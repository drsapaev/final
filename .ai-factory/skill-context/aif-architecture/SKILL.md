# `aif-architecture` Context

Read `../common/SKILL.md` first.

## Project-Specific Notes

- Keep architecture guidance aligned with the modular monolith and the `endpoint -> service -> repository -> DB` flow.
- Document cross-context service boundaries and the canonical dependency matrix from `.ai-factory/ARCHITECTURE.md`.
- If structure changes, update architecture docs and the relevant skill-context file together.

