# `aif-rules` Context

Read `../common/SKILL.md` first.

## Project-Specific Notes

- Add only short, actionable, project-specific rules.
- Prefer exact port, module, or workflow references over generic advice.
- When a rule comes from a recurring patch, keep the wording narrow enough to be enforceable.
- Rules should support the current clinic runtime and architecture, not a hypothetical simpler app.

