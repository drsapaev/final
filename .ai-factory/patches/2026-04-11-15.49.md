# Cashier API Service Payment-State Mirror Fix

## Summary
The split cashier service mirror now follows the same payment/status separation as the cashier endpoint.

## Changed
- Updated `backend/app/services/cashier_api_service.py`.
- Added `_preserve_visit_status()` to avoid storing payment completion in `Visit.status`.
- Mark-paid and confirm-payment set payment markers without overwriting operational status.
- Cancel and full refund clear `discount_mode` without forcing unrelated queue/visit status transitions.

## Validation
- `python -m py_compile C:\final\backend\app\services\cashier_api_service.py`
- `git diff --check -- backend/app/services/cashier_api_service.py`
