## Profile page hardening

- rewired `frontend/src/pages/UserProfile.jsx` into a real self-profile page backed by `GET/PUT /api/v1/authentication/profile`
- fixed backend self-profile update flow so `UserProfile` fields persist instead of silently ignoring profile-only data
- bootstrapped missing `user_profile` records when reading/updating self profile
- added `GET /api/v1/auth/csrf-token` endpoint for frontend compatibility
- switched frontend CSRF bootstrap to opt-in (`VITE_CSRF_BOOTSTRAP=1`) so dev does not spam `404 /auth/csrf-token`
- added backend regression tests for self-profile bootstrap/update and CSRF endpoint
- added frontend tests for `/profile` load/save flow

## Verification

- `python -m pytest backend/tests/test_auth_profile_api.py -q` -> `4 passed`
- `npm run test:run -- src/pages/__tests__/UserProfile.test.jsx` -> `2 passed`
- `npx eslint src/api/client.js src/pages/UserProfile.jsx src/contexts/NotificationWebSocketContext.jsx src/pages/__tests__/UserProfile.test.jsx` -> clean
- browser flow on `http://localhost:5173/profile` verified for:
  - `admin@example.com`
  - `registrar@example.com`
  - `doctor@example.com`
- live checks covered:
  - profile page load
  - edit + save + revert full name
  - notifications tab open
  - security tab open
  - no console errors / failed requests / page errors during flow
