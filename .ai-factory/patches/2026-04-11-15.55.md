# Doctor Integration API Service Payment-State Mirror Fix

## Summary
The doctor integration service mirror now follows the same payment/status separation as the endpoint.

## Changed
- Updated `backend/app/services/doctor_integration_api_service.py`.
- Removed automatic paid-payment creation from visit completion and queue completion branches.
- Removed `appointment.visit_type="paid"` writes from the service mirror.
- Existing payments still hydrate `discount_mode="paid"` and `payment_processed_at` when present.

## Validation
- `python -m py_compile C:\final\backend\app\services\doctor_integration_api_service.py`
- `git diff --check -- backend/app/services/doctor_integration_api_service.py`
