# Evolution: 2026-03-28 00:11

## Intelligence Summary

- Patches analyzed: 9 recent patches, including the AI Factory upgrade patch
- Recurring patterns: backend port drift, Postgres-first workflow drift, CORS/origin alignment, live-state persistence checks, input normalization
- Tech stack: FastAPI, SQLAlchemy, Alembic, PostgreSQL, Redis, React 18, Vite, Playwright, k6

## Improvements Applied

### ai-factory skill-context

- Added shared project context in `.ai-factory/skill-context/common/SKILL.md`.
- Added utility context overrides for best-practices, loop iteration, skill generation workflows, grounded-answer verification, and docs maintenance.
- Added skill-specific overrides for `aif`, `aif-plan`, `aif-implement`, `aif-fix`, `aif-commit`, `aif-review`, `aif-roadmap`, `aif-rules`, `aif-verify`, `aif-evolve`, `aif-docs`, `aif-architecture`, `aif-reference`, `aif-explore`, `aif-improve`, `aif-ci`, `aif-dockerize`, `aif-security-checklist`, the local `aif-deploy`, and the utility skills `aif-best-practices`, `aif-loop`, `aif-skill-generator`, and `aif-grounded`.
- Canonicalized local runtime defaults and verification norms around backend `18000`, frontend `5173`, and staging Postgres `55432`.
- Cleaned the live docs set so operational guides now use the current backend `18000` contour and no longer advertise SQLite as a current option.
- Aligned backend example/config bootstrap files so new setups now default to the current Postgres runtime contour instead of SQLite-first templates.
- Aligned backend runtime defaults and Alembic fallback behavior so code paths now resolve to the same current Postgres contour as the docs and bootstrap templates.

## Patterns Identified

- Port drift: `8000` defaults still appear in older assets, but the active contour is `18000`
- Frontend runtime: `5173` with Vite proxying API and WebSocket traffic to backend `18000`
- Staging database: `55432` is the current local staging Postgres port
- Live-state regressions: queue, cashier, and settings flows need reload-free persistence checks when they change
