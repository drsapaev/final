# AI Provider API Keys
# Получите ключи на соответствующих сайтах:
# OpenAI: https://platform.openai.com/api-keys
# Google Gemini: https://makersuite.google.com/app/apikey
# DeepSeek: https://platform.deepseek.com/api_keys

# OpenAI (рекомендуется для медицинских задач)
OPENAI_API_KEY=your_openai_api_key_here

# Google Gemini (бесплатный, хорошее качество)
GEMINI_API_KEY=your_gemini_api_key_here

# DeepSeek (дешевый, хорошее качество)
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# MCP Settings
MCP_ENABLED=true
MCP_LOG_REQUESTS=true
MCP_FALLBACK_TO_DIRECT=true
MCP_REQUEST_TIMEOUT=30
MCP_HEALTH_CHECK_INTERVAL=60
MCP_MAX_BATCH_SIZE=10

# Database
# Required. PostgreSQL only; do not use SQLite for runtime.
# Example: postgresql+psycopg://clinic:<db_password>@localhost:55432/clinicdb
DATABASE_URL=

# Auth
# Required. Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# CORS
BACKEND_CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173","http://localhost:8080","http://127.0.0.1:8080"]

# Firebase Cloud Messaging (FCM) для push уведомлений
# PR-5: используется ТОЛЬКО HTTP v1 API — аутентификация сервисным аккаунтом.
# FCM_SERVER_KEY / FCM_SENDER_ID (Legacy Cloud Messaging API) удалены из
# конфигурации: legacy API отключён Google, FCM-топики не используются.
#
# Настройка:
# 1. В Firebase Console: Project Settings > Service accounts
# 2. Нажмите "Generate new private key" — получите JSON сервисного аккаунта
# 3. Положите файл на сервер и укажите путь в GOOGLE_APPLICATION_CREDENTIALS
# 4. Роль сервисного аккаунта: Firebase Cloud Messaging API Admin
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# FCM_PROJECT_ID - обязательный, ID вашего Firebase проекта
# (Project Settings > General)
FCM_PROJECT_ID=your_fcm_project_id_here

# Включить/выключить FCM push (false = push-канал честно отключён,
# /api/v1/fcm/status показывает enabled=false)
FCM_ENABLED=false

# ==============================================================================
# F-014 + FA-012: Redis (multi-instance WS coordination, circuit breaker, arq)
# ==============================================================================
REDIS_URL=redis://localhost:6379/0
WS_REDIS_URL=redis://localhost:6379/1
ARQ_REDIS_URL=redis://localhost:6379/2

# ==============================================================================
# FA-011: Encrypted secrets (KMS alternative — no AWS/Vault needed)
# ==============================================================================
# Generate master key: python -m app.scripts.setup_secrets generate-key
# Encrypt a secret: python -m app.scripts.setup_secrets encrypt OPENAI_API_KEY "sk-..."
# If not set, falls back to environment variables (plaintext, less secure).
MASTER_KEY_FILE=/etc/clinic/master.key
SECRETS_FILE=/etc/clinic/secrets.json
