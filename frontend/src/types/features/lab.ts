// src/types/features/lab.ts
// Phase 0.5 — Laboratory domain UI types (placeholder).
// Will be filled in during Phase 5.6 (laboratory components migration).
//
// SSOT:
//   - frontend/src/components/laboratory/
//   - backend OpenAPI schemas: LabReportInstanceOut, LabOrderOut, LabResultOut, etc.

// TODO Phase 5.6: LabReportState, LabResultEntry, LabReportFormValues
export type LabReportState = Record<string, unknown>;
export type LabResultEntry = Record<string, unknown>;
