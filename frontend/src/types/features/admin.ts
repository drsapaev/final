// src/types/features/admin.ts
// Phase 0.5 — Admin domain UI types (placeholder).
// Will be filled in during Phase 5.13 (admin components migration, ~45 files).
//
// SSOT:
//   - frontend/src/components/admin/
//   - backend OpenAPI schemas: ClinicSettingsOut, BranchOut, DepartmentInfoResponse, etc.

// TODO Phase 5.13: AdminSettings, DepartmentConfig, BranchConfig
export type AdminSettings = Record<string, unknown>;
export type DepartmentConfig = Record<string, unknown>;
