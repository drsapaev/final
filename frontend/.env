# === Vite / Frontend env ===
# Имя приложения, используемое в UI
VITE_APP_NAME="Clinic Queue Manager"

# База для REST API (совместимо с backend/app/core/config.py: API_V1_STR=/api/v1)
# Пример для локальной разработки (FastAPI на 8000):
VITE_API_BASE="http://localhost:8000/api/v1"

# (опционально) База для WebSocket, если используешь realtime-очередь
# Пример: ws://localhost:8000/ws/queue
VITE_WS_BASE="ws://localhost:8000"

# (опционально) Включить подробные логи фронтенда
VITE_ENABLE_DEBUG="1"
